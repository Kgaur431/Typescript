# Typescript
Fundamentals of Typescript

## install Node.js
https://nodejs.org/en/

## install typescript
``` npm install -g typescript ts-node``` 
        ``` when we don't aware of bug in our code untill we run it, so we need typescript```
   1. install typescript globally
   2. install ts-node is a command-line tool that allow us to compile and run typescript code with one command.
   3. ts-node is an combination of typescript (tsc index.ts) and node.js (node index.js) ==> ts-node index.ts 
### install Prettier Extension
    1.  Prettier is an opinionated code formatter.

1. npm init -y
2. npm install -g
3. npm install axios


### Interface
1. In typescript, the interface are used to define the structure of an object. 

